import * as solid_js from 'solid-js';
import { GridApi, ColumnApi, GridOptions, Module } from 'ag-grid-community';

interface AgGridSolidRef<TData = any> {
    api: GridApi<TData>;
    /** @deprecated v31 - The `columnApi` has been deprecated and all the methods are now present of the `api`. */
    columnApi: ColumnApi;
}
interface AgGridSolidProps<TData = any> extends GridOptions<TData> {
    gridOptions?: GridOptions<TData>;
    ref?: AgGridSolidRef<TData> | ((ref: AgGridSolidRef<TData>) => void);
    modules?: Module[];
    class?: string;
}
interface PortalInfo {
    mount: HTMLElement;
    SolidClass: any;
    props: any;
    ref: (instance: any) => void;
}
interface PortalManager {
    addPortal(info: PortalInfo): void;
    removePortal(info: PortalInfo): void;
}
declare const AgGridSolid: <TData = any>(props: AgGridSolidProps<TData>) => solid_js.JSX.Element;

export { type AgGridSolidProps, type AgGridSolidRef, type PortalInfo, type PortalManager, AgGridSolid as default };
